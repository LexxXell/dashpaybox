# --- dash-pay microservice ---
NETWORK=testnet                 # testnet | mainnet
PORT=8090

# backend <-> service auth (matches backend DASH_AUTH_SECRET)
AUTH_SECRET=change-me
# where the service POSTs settlement callbacks (backend public webhook)
CALLBACK_URL=https://flappyforge.bebra.pro/payments/dash
CALLBACK_SECRET=change-me       # matches backend DASH_CALLBACK_SECRET

# wallet
OWNER_STORAGE_ADDRESS=          # cold/storage Dash address funds are swept to
KEYS_ENCRYPTION_SECRET=change-me # encrypts one-time private keys at rest

# oracle (fiat -> DASH)
RATE_CACHE_TTL_SECONDS=60
COINGECKO_API_URL=https://api.coingecko.com/api/v3
COINGECKO_API_KEY=
COINGECKO_DASH_ID=dash

# payment lifecycle
PAYMENT_WINDOW_SECONDS=900
DEFAULT_INSTANT_SEND=true
DEFAULT_MIN_CONFIRMATIONS=1

# storage
DB_PATH=./data/dash-pay.db

# Dash network access for the watcher: one or more evonode gRPC-web URLs
# (comma-separated). REQUIRED — without it the SDK targets localhost and fails.
# Find active testnet nodes: https://testnet.platform-explorer.pshenmic.dev/validators?isActive=true
# (DAPI gRPC-web is on port 1443).
DAPI_SEEDS=https://68.67.122.24:1443,https://158.160.14.115:1443
# Evonodes serve TLS by IP (cert won't validate) — payment validity comes from
# ChainLock/InstantLock, not TLS, so cert verification is disabled here.
NODE_TLS_REJECT_UNAUTHORIZED=0
